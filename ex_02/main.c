/*
** EPITECH PROJECT, 2023
** Untitled (Workspace)
** File description:
** main
*/

#include "ex_02.h"

int nbr_bits(char *str) {
    int result = 0;
    // Retourne le nombres de bits necessaires pour stocker la chaine de caracteres
    // 
    // Pour cela il faut calculer le nombre de caracteres de la chaine de caracteres
    // puis calculer le nombre de bits necessaires pour stocker ce nombre
    //
    return (result);

    
}

int main(void)
{
    char *str = "je fais du bitshift";

    printf("Nombre de bits = %d, Si le nombre n'est pas 4 le resultat est mauvais\n", nbr_bits(str));

    return (0);
}
